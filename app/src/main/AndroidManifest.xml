<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a2020ap2">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Apply_new_family"/>
        <activity android:name=".Link_exsisting_family_page"/>
        <activity android:name=".Link_existing_family"/>
        <activity android:name=".Find_family"
            android:label="@string/app_name"/>
        <activity android:name=".Sign_up"
            android:label="@string/app_name"/>
        <activity
            android:name=".Main"
            android:label="@string/app_name"/>
        <activity
            android:name=".Find_id"
            android:label="@string/app_name"/>
        <activity
            android:name=".Find_pw"
            android:label="@string/app_name"/>
        <activity android:name=".LoginActivity"
            />
        <activity android:name=".Check_pw"/>
        <activity android:name=".Change_info"/>
        <activity android:name=".admin_family"/>
    </application>

</manifest>